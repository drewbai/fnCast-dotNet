# Example environment variables for local development
# Copy to .env and adjust values; shell may not auto-load .env.
# For PowerShell/CMD, set these in the shell before running.

# Fixed port for Minimal API
ASPNETCORE_URLS=http://localhost:5097

# Inference options via environment (double underscore for nested keys)
Inference__Mode=Uppercase
